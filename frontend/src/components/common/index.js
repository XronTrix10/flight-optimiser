// src/components/common/index.js
// This is the main entry point for all your common components

// Import components to re-export
import Button from './Button/Button';
import Alert from './Alert/Alert';
import Spinner from './Spinner/Spinner';
import LoadingIndicator from './LoadingIndicator/LoadingIndicator';

// Export all components
export {
  Button,
  Alert,
  Spinner,
  LoadingIndicator
};

// You can also set default exports if needed
export default {
  Button,
  Alert,
  Spinner,
  LoadingIndicator
};
